# Dependencias adicionales que requieren los DAGs del taller, sobre lo que ya
# incluye la imagen base apache/airflow:3.3.1-python3.12. Versiones fijadas
# (pip freeze) tal como se validaron en las pruebas locales del proyecto.

apache-airflow-providers-postgres==7.0.1
pandas==3.0.5
requests==2.34.2
